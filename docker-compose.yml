services:
  store:
    build:
      context: ./store
    networks:
      - pomera-net-v1

  analytics:
    build:
      context: ./analytics
    networks:
      - pomera-net-v1

networks:
  pomera-net-v1:
    name: pomera-net-v1